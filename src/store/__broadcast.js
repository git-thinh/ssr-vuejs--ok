import { __config } from '../__config.js';
import { BroadcastChannel } from 'broadcast-channel';

export const __broadcast = function (m) {
    //let o = m;
    //if (o == null) o = {};
    //o.store_updated = true;

    //const bc = new BroadcastChannel(__config.event_name);
    //bc.postMessage(o);
    ////await bc.close();
    //bc.close();
}